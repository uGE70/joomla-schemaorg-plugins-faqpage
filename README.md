# FAQPage

Adds FAQPage as a new schema type in existing Joomla 5 or later schemas.

Installation: download and install the plugin as usual. Enable the plugin and set Articles as the authorized extension in the plugin options.

